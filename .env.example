# Newsletter Email Configuration
# Copy this to .env.local and update with your values

# Required: Email where newsletter subscription notifications will be sent
NEWSLETTER_DESTINATION_EMAIL=info@vaultmont.com

# Optional: Webhook URL for subscription notifications
# NEWSLETTER_WEBHOOK_URL=https://your-webhook-service.com/newsletter

# Optional: Email service configuration (choose one)

# Option 1: SendGrid (Recommended)
# SENDGRID_API_KEY=your_sendgrid_api_key
# EMAIL_FROM=noreply@yourdomain.com

# Option 2: SMTP (Gmail, Outlook, etc.)
# EMAIL_SERVICE=nodemailer
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Option 3: Mailgun
# MAILGUN_API_KEY=your_mailgun_api_key
# MAILGUN_DOMAIN=your_domain.com

# Option 4: AWS SES
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_REGION=us-east-1